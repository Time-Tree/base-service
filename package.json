{
  "name": "@timetree/base-service",
  "author": {
    "name": "Time Tree"
  },
  "version": "1.0.15",
  "dependencies": {
    "express": "^4.16.3",
    "mongoose": "^5.1.5"
  },
  "devDependencies": {
    "@types/node": "^8.0.0",
    "rimraf": "^2.6.2",
    "typescript": "^2.6.2"
  },
  "deprecated": false,
  "description": "Base routes @ base service for future applications",
  "license": "ISC",
  "main": "dist/index",
  "typings": "dist/index",
  "scripts": {
    "prepublishOnly": "npm run compile",
    "compile": "npm run clean && tsc -p .",
    "start": "tsc -w -p .",
    "clean": "rimraf dist"
  }
}
